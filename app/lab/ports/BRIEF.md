# Recorded port commitments

Mode: Operate / Read.

Extend the existing recorded habitat view with a concrete distinction: a parcel can reach its destination before its requester knows. Two one-shot commitments give the agent something finite to preserve through delayed or lost signals. Keep physical custody, acknowledgment and inherited service separate.

Show only reached request, pickup, depot acceptance, acknowledgment and service moments. Keep final attempt counts, spare preservation, safety and the whole contract verdict at the terminal frame. A repeated send is neither an extra parcel nor evidence that a reply arrived. Make failed commitments readable even when ordinary service passes.

Reuse the incumbent map, tick slider, saved cuts, keyboard controls, export links, flat sections and restrained typography. No browser simulation, multiplayer economy, arbitrary request IDs, or general distributed-systems guarantee. Browser proof must cover each moment's exact boundary, failed acknowledgment with successful delivery, restore equality, immutable downloads, selection races, recovery from malformed data, mobile text zoom and the unchanged older views.
